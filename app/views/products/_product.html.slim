tr
  td = product.id
  td = link_to product.name, product_path(product), data: { turbo_frame: '_top' }
  td = product.category.name
  td = product.price
  td = product.quantity
  td = l(product.created_at, format: :medium)
